*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
app/build/

# Release signing — never commit keystores or credentials
*.jks
*.keystore
keystore.properties

# On-device AI model — large binary, fetched from HuggingFace, never committed
aimodel/src/main/assets/*.litertlm
aimodel/build/
